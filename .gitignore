# Dart / Pub
.dart_tool/
.packages
build/
.pub-cache/

# Environment - NEVER commit secrets
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.iml

# Claude Code local config
.claude/

# OS
.DS_Store
Thumbs.db

# Compiled output
bin/server

# Real data exports — schema.sql (structure only) stays tracked, but any
# dump containing actual rows must never be committed again.
db/u524585165_lifecare*.sql
db/*dump*.sql
